# Install requirements for Julia
cd to the directory where rpackages.jl is located

run: julia packages.jl

# Install requirements for Python (pip or pip3 for Python 3 depending on your system settings)
cd to the directory where requirements.txt is located

activate your virtualenv (or install it to the system)

run: pip3 install -r requirements.txt in your shell